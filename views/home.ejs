<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>

    <title>Blog</title>

    <style>
      body {
          background: linear-gradient(135deg, #f8fafc 0%, #e0e7ff 50%, #f0abfc 100%);
      }
       
        .card-img-top {
            height: 200px;
            object-fit: cover;
        }

        .card-body {
            padding: 1.5rem;
        }

        .card-title {
            font-size: 1.25rem;
            margin-bottom: 0.75rem;
        }

        .card-text {
            overflow: hidden;
            text-overflow: ellipsis;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
        }
        </style

</head>

<body class="bg-light">
  <%- include('./partials/nav') %>

  <div class="container py-5">
    <div class="mx-auto" style="max-width: 800px;">
      <% blogs.forEach(blog => { %>
        <div class="card mb-4 shadow-sm rounded-3 overflow-hidden">
          <img src="<%= blog.coverImageURL %>" class="card-img-top" alt="cover" style="height: 200px; object-fit: cover;">

          <div class="card-body">
            <h5 class="card-title"><%= blog.title %></h5>
            <p class="card-text text-muted" style="overflow: hidden; text-overflow: ellipsis; display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical;">
              <%= blog.body %>
            </p>
            <a href="/blog/<%= blog._id %>" class="btn btn-primary btn-sm mt-2">Read Blog</a>

            <% if (blog.createdBy) { %>
              <div class="d-flex align-items-center gap-2 mt-3">
                <!-- <img src="<%= blog.createdBy.profileImageURL || '/default-avatar.png' %>" class="rounded-circle" width="40" height="40" style="object-fit: cover;" /> -->
                <span class="text-muted small"><%= blog.createdBy.fullName %></span>
              </div>
            <% } else { %>
              <p class="text-muted small mt-3">Author unknown</p>
            <% } %>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <%- include('./partials/scripts') %>
</body>

<footer>
  <div class="footer" style="background-color: #748ba1; border-top: 1px solid #dee2e6;">
    <div class="container text-center py-3">
      <p class="text-muted mb-0">© 2025 Blogify. All rights reserved.</p>
      <p class="fw-bold text-white mb-2" style="font-size: 1.1rem;">
        <span style="color: #f0abfc;">Explore</span> Node.js: Bindings,Restful API's ,MVC, CRUD ,Database interactoin , JWT Auth, Routing, Schemas &amp; more — <span style="color: #9c271f;">Full-Stack Backend Power!</span>
      </p></p>
      <p> Shahsvatgodne123@gmail.com</p>
  </div>
</footer>


</html>